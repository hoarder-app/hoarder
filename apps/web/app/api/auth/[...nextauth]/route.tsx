import { authHandler } from "@/server/auth";

export { /* @next-codemod-error `authHandler` export is re-exported. Check if this component uses `params` or `searchParams`*/
authHandler as GET, /* @next-codemod-error `authHandler` export is re-exported. Check if this component uses `params` or `searchParams`*/
authHandler as POST };
