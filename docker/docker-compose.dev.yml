services:
  web:
    build:
      dockerfile: Dockerfile.dev
    volumes:
      - data:/data
      - ..:/app
      - modules:/app/node_modules
    ports:
      - 3000:3000
    env_file:
      - .env  # Automatically load all variables from this file
    command:
      - pnpm
      - web
    working_dir: /app
    depends_on:
      prep:
        condition: service_completed_successfully
  chrome:
    image: gcr.io/zenika-hub/alpine-chrome:123
    restart: unless-stopped
    command:
      - --no-sandbox
      - --disable-gpu
      - --disable-dev-shm-usage
      - --remote-debugging-address=0.0.0.0
      - --remote-debugging-port=9222
      - --hide-scrollbars
  meilisearch:
    image: getmeili/meilisearch:v1.11.1
    volumes:
      - meilisearch:/meili_data
  workers:
    build:
      dockerfile: Dockerfile.dev
    volumes:
      - data:/data
      - ..:/app
      - modules:/app/node_modules
    working_dir: /app
    environment:
      MEILI_ADDR: http://meilisearch:7700
      BROWSER_WEB_URL: http://chrome:9222
      DATA_DIR: /data
      # OPENAI_API_KEY: ...
    command:
      - pnpm
      - workers
    depends_on:
      prep:
        condition: service_completed_successfully
  prep:
    build:
      dockerfile: Dockerfile.dev
    working_dir: /app
    environment:
      DATA_DIR: /data
    volumes:
      - data:/data
      - ..:/app
      - modules:/app/node_modules
    command:
     - /bin/sh
     - -c
    #  - "pnpm install && pnpm run db:migrate"
     - "pnpm install --frozen-lockfile && pnpm run db:migrate" # frozen flag messes with m1 docker builds

volumes:
  meilisearch:
  data:
  modules:
